---
kind: decision
date: 2026-07-20
---

# Site exposes generic endpoint configuration

The PDF site projection lets a publisher add ordinary endpoint locators and
select a supported delivery profile for each. It may offer presentation-only
examples, but no suffix is special and the browser never auto-generates or
interprets an endpoint. Server-returned configuration remains authoritative.
