---
kind: decision
date: 2026-07-20
---

# HTTP accepts endpoint configuration; it never manufactures paths

The PDF upload metadata must carry the publisher-supplied endpoint locator and
delivery-profile set. The adapter validates shape and maps typed application
outcomes, but never appends `.pdf`, chooses a second URL, or infers disposition
from a path. Responses return the accepted configured endpoints.
