---
kind: review
date: 2026-07-17
---

# Attached-content follow-up review

The exclusive-control scan found two interchangeable-content groups:
Markdown/CSS and Raw/Steps. Both now use the reusable
`ExclusiveContentSwitcher`, physically meet the controlled panel, merge the
selected tab into its edge, and preserve existing source/mode state.
Split/full-width remains deliberately detached and labelled as layout.
Fullscreen and Compact/Whole are independent toggles, while section type mutates
a draft schema; none incorrectly receive tab treatment.

Accessibility now matches the visual model: tablist/tab/tabpanel roles,
`aria-selected`, `aria-controls`, selected-tab roving focus, and wrapping
Arrow/Home/End keyboard activation. A rejected unsaved mode change restores
focus to the still-selected tab.

Desktop, mobile, Raw, Steps, and CSS states were visually checked. Browser
interaction checks confirm source and mode switching, controlled panel labels,
hidden inactive panes, preserved Steps state across CSS switching, and keyboard
selection/focus. No blocking findings remain.

Final verification: `deno task check`, `deno task test` (102 passed), and
`deno task build`.
