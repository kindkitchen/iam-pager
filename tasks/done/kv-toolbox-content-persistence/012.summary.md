---
kind: summary
date: 2026-07-20
---

# Controlled v2 composition completes the replacement

The explicit `deno-kv-v2` page profile now inherits the durable ownership path,
opens the project KV gateway, runs the strict source-bound v2 readiness probe,
and supplies `KvPageAggregateRepository` through the named aggregate interface.
Missing, changed, corrupt, conflicting, or incomplete migration state fails
closed; failed selection closes its unused gateway and never runs migration.

The retained `deno-kv` profile continues to select the untouched schema-v1 raw
repository, preserving the service compatibility path and deliberate fallback
window. Memory composition remains unchanged. Configured service coverage proves
the aggregate path, and factory coverage proves inherited path selection,
unmigrated refusal, gateway cleanup, migrated-page visibility, and v1 fallback.

README, API documentation, specifications, and changelog now document operator
order and the exact profile semantics. All 543 tests, tracked-source formatting,
lint, type checks, production build, and a disposable manual schema
initialize/update/check flow pass. The kv-toolbox replacement is complete; PDF
HTTP work is unblocked.
