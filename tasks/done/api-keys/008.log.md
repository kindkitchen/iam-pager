---
kind: log
date: 2026-07-22
---

# Specification freeze and closure (plan links 1 and 7); task complete

Closed the remaining documentation and regression boundary:

- Specifications now carry the API-key invariants: new `SA-APIKEY` (credential
  model, hash-only persistence, bearer precedence, browser-owned management),
  `CP-APIKEY` and `CP-API-AUTH` capabilities, `EX-AUTOMATE` experience;
  updated `PD-CREATOR`, `QT-AUTHORITY` (bearer authority, no cookie fallback,
  no cookie issuance), `QT-API` (two credentials), `QT-VERIFY` (key coverage),
  `OS-LIMITS` (no key rate limits), `SA-STORAGE`, and `SP-CORE`/spec README.
- New `docs/api/authentication.md`: principal classes, credential precedence,
  CSRF policy, stable errors, and the complete per-endpoint permission matrix.
  `docs/api/pages.md` and `docs/api/api-keys.md` cross-reference it; the stale
  "until the resolver lands" paragraph in the key contract is removed.
- README: `## API keys` section with curl bearer examples, permission
  semantics, browser-owned management boundary, and a security warning
  (header-only transport, narrow grants, expiry, revocation).
- New `lib/api-contract-matrix.test.ts`: table-driven matrix over every page,
  namespace, and key-management endpoint × principal class (browser owner,
  guest browser, mapped-permission key, under-privileged key, revoked key,
  invalid explicit bearer), plus bearer revoke-all permission gating and
  self-invalidation. Stale `If-Match`/revision values keep allowed mutation
  rows non-destructive. 551 tests total.
- Stale-claim and redaction sweep found nothing: no doc claims keys do not
  exist or that every mutation needs CSRF; no fixture transports a bearer in
  query or body.

Gates: `deno task check`, `deno task test` (551), `deno task build` all pass.
Every plan link (1–7) is complete; status active -> done.
