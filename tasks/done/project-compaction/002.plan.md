---
kind: plan
date: 2026-07-21
---

# Prune around the current aggregate model

The authoritative product is deterministic namespace/page delivery, guest publishing, creator ownership and management, public exploration, Markdown/PDF content, and configurable endpoint profiles. Keep interface-backed memory and Deno KV implementations plus thin HTTP/Fresh projections.

Remove the superseded schema-v1 page repository, its compatibility branch in `PageService`, v1-to-v2 migration/readiness machinery, generic database-release framework, fallback backend selector, and tests/documentation that protect those transitions. Make the current page aggregate repository the only page persistence interface for both memory and Deno KV. Rewrite README/specifications as current product and architecture statements rather than delivery history, while retaining the current API behavior and regression coverage.
