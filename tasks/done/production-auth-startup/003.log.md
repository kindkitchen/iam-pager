---
kind: log
date: 2026-07-18
---

# Completed production startup repair

Status: active -> done. The SSR build now externalizes gauth and Effect only for
server rendering, removing the circular preset-chunk evaluation while retaining
eager configuration validation. A production runner parses optional `PORT`
before loading the generated server and leaves Deno's port-8000 default
unspecified when absent.

Verified the full 168-test suite, formatting/lint/type checks, production build,
invalid-port failure, explicit and default ports, and built-server startup in
both local and original Google modes. README, authentication specification, and
changelog now document local production startup and Deno Deploy requirements.
