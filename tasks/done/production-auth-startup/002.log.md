---
kind: log
date: 2026-07-18
---

# Activated production startup repair

Status: todo -> active. Reproduced the built-server deadlock in both local and
original Google modes. The separate port issue is also confirmed: options passed
after the existing `start` task are forwarded as module arguments, so they do
not change `deno serve` from port 8000.
