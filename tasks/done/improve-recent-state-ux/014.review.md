---
kind: review
date: 2026-07-17
---

# Client-only preview review: approved

No blocking findings.

- Preview rendering is synchronous and fully local through browser-compatible
  `marked`; there is no Fetch call or preview route.
- Draft HTML and CSS are intentionally not sanitized in the preview. The iframe
  retains an empty sandbox, while `MdPageHandler` remains the authoritative
  publish-time sanitizer and renderer.
- The published result link no longer opens a new tab, so navigating to raw
  locator content adds a normal same-tab history entry and Back returns to the
  editor. Raw locator responses remain unchanged.
- Markdown starts with `# Your page` and the concise workflow tip
  `Write. Style. Preview. Publish.`; the old placeholder is removed.
- Client modules contain no `node:*`, `MdPageHandler`, `@deno/gfm`, sanitizer,
  Fetch, or preview-endpoint references.
- `deno task check`, all 78 tests, production build, development module
  inspection, and headless Firefox rendering passed without module/MIME errors.
