---
kind: log
date: 2026-07-19
---

# Exposed expanded management through HTTP

Completed the transport milestone over the existing HTTP-independent contracts.

- Managed create and PATCH accept optional bounded tags; summaries expose
  canonical tags; managed list accepts AND-combined name, access, and exact-tag
  filters with cursor scope retained by the service.
- Added strict authenticated, CSRF-protected, revision-bound rename and bodyless
  duplicate actions. Both return current inspection representations and ETags;
  duplicate also returns the new management location.
- Added strict authenticated bulk access and deletion routes. Commands decode
  1-100 page/revision selections, preserve all-before-write service validation,
  and return ordered independent success, stale, exhausted, or non-disclosing
  missing outcomes.
- Exposed exact-tag public exploration through the site GET form, retained the
  tag in continuations, and projected canonical result tags without management
  identity.
- Updated README, the complete page API contract, CHANGELOG, and DS-MANAGE,
  CP-MANAGE, EX-MANAGE, exploration, addressing, authentication, and technical
  specifications.

Validation: `deno task check`, all 424 tests, and `deno task build` pass. The
Fresh build includes all four new action/bulk routes.

Next: extend the web-independent creator management projection and island with
managed filters, tag editing, rename, duplicate, explicit selection, and bulk
access/delete controls.
