---
kind: log
date: 2026-07-18
---

# Contracts delivered; status active -> done

Implemented `lib/namespace/` per [[001.draft]]:

- `model.ts` — `NamespaceReservation` (namespace as supplied, owner_user_id,
  reserved_at) and `namespace_key`, derived via the locator model's
  `locator_key` so reservation identity can never diverge from locator
  identity (DA-NAMESPACE, DA-LOCATOR).
- `interfaces.ts` — `NamespaceRepository` (reserve / find / list_by_owner)
  with the contract documented on the interface: atomic reserve with exactly
  one winner under concurrency and typed `taken` for losers; case-insensitive
  identity with supplied casing preserved; `reserved_at` stamped by the
  implementation; namespace validity explicitly left to the service layer via
  the locator engine.
- `memory-repository.ts` — Map-backed implementation; atomicity holds because
  check-then-set is synchronous (no await between check and set).
- `conformance.ts` — `test_namespace_repository_conformance`, an
  implementation-agnostic suite (factory + optional teardown) that registers
  the contract rules as Deno tests; `durable-storage` backends reuse it
  unchanged.

All done-when criteria pass: reserve, duplicate rejected across casings,
supplied casing preserved, owner listing, concurrent reserve yields one
winner (plus injected-clock test in the memory test file). 9 new tests,
186 total; fmt/lint/check clean.

Next: direction step 2 queued as `namespace-reservation-service`
(reservation service + publishing authorization).
