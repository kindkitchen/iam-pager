---
kind: summary
date: 2026-07-21
---

# PDF site projection through creator management

The task is active at chain position 5 of 5. Durable v2 persistence and the PDF
HTTP contract are completed dependencies and remain contract-only from the UI.

Implemented site work:

- a raw Markdown/PDF choice and strict multipart create-request boundary;
- logical breadcrumbs and the separate `/site/manage` creator route;
- bounded advisory PDF selection with explicit ordinary endpoint/profile intent;
- selected-file and draft retention across publish failures;
- typed endpoint, PDF, size, authority, stale, availability, and request failure
  presentation that discards unknown server detail;
- creator PDF profile-derived preview/download links, bounded metadata inspection
  without bytes, and exact-ETag/CSRF multipart replacement with no stale retry.

The next coherent step is the public browser-native PDF wrapper: derive preview
and attachment links from returned endpoint profiles, embed the canonical inline
URL with a visible unsupported-browser fallback, and keep Back/direct/download
navigation usable. Then verify exploration renders one logical PDF row with type
and size and complete responsive/browser acceptance. PDF.js, generic binary UI,
external storage, storage-key knowledge, and HTTP/persistence ownership remain
out of scope.
