---
kind: log
date: 2026-07-21
---

# Active to done — complete the public PDF site experience

Completed the final public browser-native wrapper step. The raw presenter now
derives a canonical inline preview and attachment downloads only from returned
endpoint profiles, and the Fresh projection embeds that local PDF endpoint while
keeping Back, direct-open, download, and unsupported-browser fallback links
outside the viewer. No PDF bytes enter the page model or serialized wrapper.

Public exploration component coverage verifies one logical PDF row with content
type and size despite its multiple delivery endpoints. Responsive mobile and
desktop Chromium acceptance exercised a real multipart guest publication and
the composed `/site` wrapper. All 579 tests, formatting, lint, type checks, and
the production build pass.

Status: `active -> done`; all five chain positions and the defined PDF site scope
are complete. PDF.js, generic binary UI, external storage, and storage/HTTP
ownership remain separate later work.
