---
kind: draft
date: 2026-07-24
---

# Fix repeat Google sign-in redirect mismatch

A previously authenticated browser can log out successfully, but its next Google sign-in reaches Google's `redirect_uri_mismatch` error without an account-selection prompt. Trace the exact outbound `redirect_uri`, make production callback selection stable across logout and subsequent guest sessions, add regression coverage for repeated starts, and keep deployment guidance current.
