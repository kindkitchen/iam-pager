---
kind: summary
date: 2026-07-20
---

# Content endpoint bindings completed

The task established one logical `PageAggregate` with one immutable
`ContentAsset` reference and a complete endpoint set containing one explicit
canonical binding plus up to seven deterministic alternates. A pure planner
validates ordinary same-namespace locators and content-supported `inline` or
`attachment` profiles. Focused persistence capabilities and the process-local
reference provide staged asset creation, atomic endpoint claims/moves,
revision-bound combined mutation, safe immutable sharing, endpoint-wide delete,
and logical-page owner/public queries with backend-neutral conformance.

`PageService` composes the current `md-page` behavior through those split
capabilities while retaining the raw-Deno-KV legacy path pending Kvdex. It stages
validated assets before visibility, flips references atomically, resolves every
direct locator through a binding, and returns one canonical logical row.
Owner/public summaries expose complete safe endpoint links while keeping the
canonical compatibility fields. Direct HTTP uses the resolved profile for
content disposition and never infers behavior from a suffix or filename.

All 479 tests, check, and the production build pass. The next chain task is
`pdf-content-core`; persistence, PDF HTTP, and PDF site work remain separate
planned tasks.
