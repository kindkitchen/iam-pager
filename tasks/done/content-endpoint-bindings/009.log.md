---
kind: log
date: 2026-07-20
---

# Complete endpoint links and selected delivery

Status `active -> done`: the generic page/content/endpoint prerequisite is
complete and the PDF-specific chain can build on it.

Owner and public summaries retain canonical `locator`/`path` compatibility
fields and now expose the complete canonical/alternate endpoint set as detached,
safe app-relative links. Management, wrapped-view, and exploration projections
show configured alternates without changing one-page query cardinality. The
legacy raw-Deno-KV path projects its existing canonical inline endpoint.

Direct resolution carries the exact selected endpoint through the application
contract. Delivery rejects bindings unsupported by the content handler and maps
the stored profile, rather than path suffixes or filename hints, to inline or
attachment disposition; attachment filename encoding remains bounded and safe.

Verification: all 479 tests, `deno task check`, and `deno task build` pass.
