---
kind: log
date: 2026-07-18
---

# Reopened authentication for one final acceptance step

Status: done -> active. Review against [[001.draft]] showed that persistent
namespace ownership is correctly a separate task, but the authentication task
was closed before every original phase-4/phase-5 criterion was evidenced.

## Final step — close authentication acceptance

1. Extend the configured local browser integration through authenticated
   `POST /auth/logout`: publish the fresh guest cookie, reject the stale
   authenticated bearer, and prove the new guest session is distinct.
2. Add a small provider-neutral callback failure presentation with a safe retry
   link. Verify the failed attempt is not reusable and unrelated guest session
   state survives; keep callback values and provider causes out of the response.
3. Settle `OQ-AUTH` around Google-first account creation, guest-to-authenticated
   upgrade, provider-owned recovery, and logout-to-fresh-guest behavior. Align
   `CP-NAMESPACE` and `QT-AUTHORITY` without claiming namespace ownership is
   implemented.
4. Record a local browser smoke through sign-in and logout, then run the full
   check, test, and production build gates.

When these pass, move `user-authentication` back to done. Saved-profile display,
account/profile/settings navigation, and responsive account-menu behavior remain
optional follow-up UI work. Namespace ownership and authenticated publishing are
a separate subsequent task.
