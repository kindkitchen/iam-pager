---
kind: log
date: 2026-07-22
---

# Web management projection landed (plan link 6)

Added the secondary web projection for owner API keys. All logic stays in raw
code; the key subsystem in `lib/api-key/` is untouched and the page is a thin
client of `/api/api-keys`:

- `lib/ui/api-key-panel.ts`: `CreatorApiKeyPanelPresenter` (hidden for guests,
  serializable creator model with preformatted `If-Match` validators, no owner
  ID, no secret material), draft violation advisory mirroring domain rules,
  strict request builders for list/create/update/revoke/revoke-all, strict
  response decoders, and stale-aware failure mapping.
- `islands/ApiKeyPanel.tsx`: generate form (label, permission checkboxes with a
  full-access `all` shortcut, optional `datetime-local` expiry), one-time
  bearer reveal with clipboard copy + "I stored it" dismiss, inline edit with
  revision-bound save, two-step confirm for individual revoke and revoke-all,
  412/428 recovery by list refresh. The bearer lives only in transient island
  state.
- `components/ManageApiKeysPage.tsx` + thin route `routes/site/api-keys.tsx`;
  navigation destination "API keys" and breadcrumb location `api_keys`;
  `api_key_panel` wired in `lib/app.ts`; panel styles in `static/site.css`.
- Repo hygiene: fixed pre-existing gate failures — `static/site.css`
  formatting and the `CssSourceEditor` `setTimeout` return type — so
  `deno task check` and `deno task test` pass fully again (500 tests).

Remaining links: 1 (specification documents), 3 (Deno KV repository +
conformance + storage factories), 5 (API principal resolver over
page/namespace operations), 7 (regression and documentation closure).
